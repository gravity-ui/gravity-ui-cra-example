{
  "name": "cra-template-gravity-ui-pure",
  "version": "1.0.0",
  "keywords": [
    "react",
    "create-react-app",
    "template",
    "typescript",
    "gravity",
    "uikit",
    "sass"
  ],
  "description": "CRA template with: typescript, sass and @gravity-ui packages",
  "repository": {
    "type": "git",
    "url": "https://github.com/gravity-ui/cra-template-gravity-ui"
  },
  "engines": {
    "node": ">=18"
  },
  "bugs": {
    "url": "https://github.com/gravity-ui/cra-template-gravity-ui/issues"
  },
  "files": [
    "template",
    "template.json"
  ],
  "scripts": {
    "prepare": "husky install",
    "check-template": "rm -rf test-app && npx create-react-app test-app --template file:. && cd test-app && npm run lint && cd ../ && rm -rf test-app",
    "test": "exit 0"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
